```
└───doc   							// jsdoc generated documentation
└───example 						// Simple example project that utilizes the sdk
└───build 							// Js compiled code from ts files
└───src 								// Files relevant to the project
		└───common						// Files that are imported in different locations to support several utility functions
		└───errors						// All error classes that the sdk supports
		└───interfaces				// All interfaces from the sdk
		└───resources					// Contains a folder for each resource, and each resource contains at leas a core a a test file

```